# Service Ports
APP_PORT=8000
USER_SERVICE_PORT=8001

# Ollama Configuration - EXTERNAL (remote server)
# Leave COMPOSE_PROFILES empty to skip local Ollama container
COMPOSE_PROFILES=

# IMPORTANT: Update these values for your external Ollama instance
OLLAMA_HOST=https://your-ollama-server.com
MODEL_NAME=llama3.2:3b

# Steps to use:
# 1. Copy this file: cp .env.external.example .env.external
# 2. Edit .env.external with your actual Ollama URL and available model
# 3. Run: make use-external-ollama && make restart
#
# To find available models on your Ollama server:
#   curl https://your-ollama-server.com/api/tags
