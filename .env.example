# Service Ports
APP_PORT=8000
USER_SERVICE_PORT=8001

# Ollama Configuration
# Choose one of the configurations below by copying this file:
#   cp .env.example .env
# Then uncomment the configuration you want to use

# Option 1: Using LOCAL Ollama (Docker container)
# COMPOSE_PROFILES=local-ollama
# OLLAMA_HOST=http://ollama:11434
# MODEL_NAME=llama3.2

# Option 2: Using EXTERNAL Ollama (remote server)
# COMPOSE_PROFILES=
# OLLAMA_HOST=https://your-ollama-server.com
# MODEL_NAME=llama3.2:3b

# Quick Setup:
# For local:    cp .env.local.example .env.local && make use-local-ollama
# For external: cp .env.external.example .env.external && edit .env.external && make use-external-ollama
